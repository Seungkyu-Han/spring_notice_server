spring:
  cloud:
    aws:
      credentials:
        access-key: ${accessKey}
        secret-key: ${secretKey}

      region:
        static: ap-northeast-2

      sns:
        topic:
          arn: ${arn}
      sqs:
        queue:
          name: study